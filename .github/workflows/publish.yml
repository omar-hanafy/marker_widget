name: Publish to pub.dev

on:
  push:
    tags:
      - 'marker_widget-v*'

permissions:
  contents: read

jobs:
  publish:
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
